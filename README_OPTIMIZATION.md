# 🚀 Быстрая оптимизация бэкенда до < 100 МБ

## Текущая ситуация
- **Полный размер:** ~374 МБ
- **node_modules:** ~373 МБ (включая dev зависимости)
- **Цель:** < 100 МБ

## ⚡ Быстрый способ (Windows PowerShell)

```powershell
.\optimize.ps1
```

## ⚡ Быстрый способ (Linux/Mac)

```bash
chmod +x optimize.sh
./optimize.sh
```

## 📋 Ручная оптимизация

### Шаг 1: Production сборка
```bash
npm run build:prod
```

### Шаг 2: Установка только production зависимостей
```bash
npm ci --production
```

### Шаг 3: Очистка (опционально)
Удалите:
- `src/` - исходный код
- `test/` - тесты  
- `tsconfig*.json` - конфигурация TypeScript
- `nest-cli.json` - конфигурация NestJS
- `eslint.config.*` - конфигурация ESLint

## 🐳 Docker оптимизация (рекомендуется)

Используйте `Dockerfile.optimized` для multi-stage build:

```bash
docker build -f Dockerfile.optimized -t backend-optimized .
```

Это создаст образ размером ~150-200 МБ (зависит от базового образа).

## 📊 Ожидаемые результаты

После оптимизации:
- **node_modules (production):** ~150-200 МБ
- **dist:** ~1-2 МБ  
- **prisma:** ~5-10 МБ
- **Итого:** ~160-210 МБ

**Для достижения < 100 МБ:**
- Используйте Docker multi-stage build с `node:alpine`
- Или удалите неиспользуемые зависимости вручную
- Рассмотрите альтернативные легковесные библиотеки

## ⚠️ Важно

Не удаляйте:
- ✅ `dist/` - скомпилированный код
- ✅ `node_modules/` - зависимости
- ✅ `prisma/` - схема базы данных
- ✅ `package.json` и `package-lock.json`
- ✅ `.env` файлы

## 🔍 Проверка размера

**Windows:**
```powershell
(Get-ChildItem -Recurse -File | Measure-Object -Property Length -Sum).Sum / 1MB
```

**Linux/Mac:**
```bash
du -sh .
```

