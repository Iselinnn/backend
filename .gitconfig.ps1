# Add this to your PowerShell profile to auto-fix Git PATH
# Run: notepad $PROFILE
# Then add: . "$PSScriptRoot\fix-git-path.ps1"

# Or run this script manually when Git doesn't work:
# . .\fix-git-path.ps1

